import Link from "next/link";
export default function ContactCTA({label="Contact",title="Have an opportunity worth discussing?",subtitle="Tell us what you are building, funding, or trying to move forward.",buttonText="Start the Conversation"}:{label?:string;title?:string;subtitle?:string;buttonText?:string}){return <section id="contact" className="section bg-[#f6f7f4]"><div className="container split items-center"><div><p className="eyebrow">{label}</p><h2 className="display text-[clamp(2.7rem,5vw,4.7rem)] leading-[1.02]">{title}</h2></div><div><p className="prose">{subtitle}</p><Link href="/contact" className="button-primary mt-7">{buttonText}</Link></div></div></section>}
