"use client";
import Link from "next/link";import {useEffect,useRef,useState} from "react";import Logo from "./Logo";
const links=[["Home","/"],["About","/about"],["Services","/#services"],["Support","/support"],["Contact","/contact"]] as const;
export default function SiteNav(){const[open,setOpen]=useState(false);const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(open)closeRef.current?.focus();const onKey=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);document.addEventListener("keydown",onKey);document.body.style.overflow=open?"hidden":"";return()=>{document.removeEventListener("keydown",onKey);document.body.style.overflow=""}},[open]);return <header className="relative z-50 bg-white"><div className="container flex h-[82px] items-center justify-between"><Logo/><nav aria-label="Primary" className="hidden items-center gap-8 lg:flex">{links.map(([label,href])=><Link key={label} href={href} className="text-[.69rem] font-semibold uppercase tracking-[.1em] text-[#272b28] hover:text-[#07583f]">{label}</Link>)}<Link href="/contact" className="button-primary ml-3 !min-h-[42px] !bg-white !text-[#07583f] hover:!bg-[#f3f7f4]">Get in touch</Link></nav><button type="button" onClick={()=>setOpen(true)} aria-expanded={open} aria-controls="mobile-menu" className="min-h-11 min-w-11 border border-[#d7ddd8] text-xs font-semibold uppercase tracking-wider lg:hidden">Menu</button></div>{open&&<div id="mobile-menu" role="dialog" aria-modal="true" aria-label="Navigation" className="fixed inset-0 z-50 bg-white p-5 lg:hidden"><div className="flex items-center justify-between"><Logo/><button ref={closeRef} type="button" onClick={()=>setOpen(false)} className="min-h-11 min-w-11 border border-[#d7ddd8] text-xl" aria-label="Close navigation">×</button></div><nav aria-label="Mobile" className="mt-16 flex flex-col border-t border-[#dde1dd]">{links.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className="border-b border-[#dde1dd] py-5 font-serif text-3xl">{label}</Link>)}</nav></div>}</header>}
